# oh-box-zsh

从 0 开始构建的 zsh 配置框架 不依赖 oh-my-zsh

## 安装

### link 模式

```bash
./install.sh --link --force
```

### copy 模式

```bash
./install.sh --copy --force
```

## 本地覆盖点

以下文件默认不进版本控制：

- `zsh/local.features.zsh`
- `zsh/local.zsh`
